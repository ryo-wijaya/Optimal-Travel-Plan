import { PaymentTransactionHandler } from './payment-transaction-handler';

describe('PaymentTransactionHandler', () => {
  it('should create an instance', () => {
    expect(new PaymentTransactionHandler()).toBeTruthy();
  });
});
