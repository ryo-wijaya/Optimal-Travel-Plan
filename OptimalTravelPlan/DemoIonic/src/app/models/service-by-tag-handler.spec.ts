import { ServiceByTagHandler } from './service-by-tag-handler';

describe('ServiceByTagHandler', () => {
  it('should create an instance', () => {
    expect(new ServiceByTagHandler()).toBeTruthy();
  });
});
