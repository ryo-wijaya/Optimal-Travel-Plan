import { PaymentAccountHandler } from './payment-account-handler';

describe('PaymentAccountHandler', () => {
  it('should create an instance', () => {
    expect(new PaymentAccountHandler()).toBeTruthy();
  });
});
